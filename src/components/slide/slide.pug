
mixin slide(slide)
  .slide(data-class="full", style={'background-image': `url('/images/${slide.url}')`})
    +bg-shadow()
    .slide-info
      .slide-title= slide.title
      .slide-text= slide.text
      +button('Learn more')(class="slide-button")